# ============================================================
# KANZLEI AI — Python Dependencies v3.0
# pip install -r requirements.txt
# Python: 3.11+
# ============================================================

# ─── API Framework ───────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6         # Inkl. websockets, httptools
python-multipart==0.0.9           # File-Uploads
websockets==12.0                  # WebSocket-Support

# ─── Data Validation ─────────────────────────────────────────
pydantic==2.8.2
pydantic[email]==2.8.2
email-validator==2.2.0

# ─── Environment & Config ────────────────────────────────────
python-dotenv==1.0.1

# ─── HTTP / Async (für KI-API-Calls) ─────────────────────────
httpx==0.27.2                     # Async HTTP — für Claude API
aiofiles==23.2.1                  # Async File I/O

# ─── KI / Anthropic ──────────────────────────────────────────
anthropic==0.34.0                 # Offizielles SDK (Optional — wir nutzen httpx direkt)

# ─── ML & Datenbank ──────────────────────────────────────────
# sqlite3 ist in Python stdlib — kein extra Paket
# SQLite für ML-Buchungsassistent: core/ml_buchung.py

# ─── Export ──────────────────────────────────────────────────
openpyxl==3.1.5                   # Excel-Export
xlsxwriter==3.2.0                 # Excel mit Charts

# ─── Email ───────────────────────────────────────────────────
# smtplib in Python stdlib
# Für professionelle HTML-Emails:
jinja2==3.1.4                     # Email-Templates

# ─── Datum & Zeit ────────────────────────────────────────────
python-dateutil==2.9.0
pytz==2024.1

# ─── Sicherheit ──────────────────────────────────────────────
passlib[bcrypt]==1.7.4            # Passwort-Hashing
python-jose[cryptography]==3.3.0  # JWT-Token
bcrypt==4.1.3

# ─── Utilities ───────────────────────────────────────────────
python-magic==0.4.27              # MIME-Type Erkennung für Uploads
pillow==10.4.0                    # Bild-Verarbeitung (OCR Vorbereitung)

# ─── Redis (Session-Cache, Rate-Limiting) ────────────────────
redis==5.0.8
aioredis==2.0.1

# ─── Logging & Monitoring ────────────────────────────────────
structlog==24.4.0                 # Strukturiertes Logging
sentry-sdk[fastapi]==2.13.0       # Error-Tracking (Optional)

# ─── PDF Generation ──────────────────────────────────────────
# Für Lohnzettel + Rechnungs-PDFs:
# weasyprint==62.3                # Vollständiges HTML→PDF (schwer, needs libcairo)
# Alternativ: Wir nutzen HTML-Output + Browser-Druck

# ─── DATEV / Bank ────────────────────────────────────────────
# python-fints==3.3.0             # FinTS/HBCI Bank-Zugriff (Optional)

# ─── PostgreSQL (Optional — für >100 Mandanten) ──────────────
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9           # SQLite -> PostgreSQL Migration Script

# ─── Dev & Testing ───────────────────────────────────────────
# pytest==8.3.2
# pytest-asyncio==0.23.8
# httpx==0.27.2                   # Bereits oben
# black==24.8.0
# ruff==0.6.2